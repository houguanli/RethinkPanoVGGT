defaults:
  - pano_luna.yaml
  - _self_

# Parameter-matched "without Patch Bank" control. The Patch GeoRA adapter,
# spherical position encoding, hidden width, and residual scale are unchanged;
# only the cross-window bank context is replaced by zeros.
exp_name: pano_luna_ablate_patch_bank

model:
  enable_luna: True
  luna_patch_bank_mode: none
