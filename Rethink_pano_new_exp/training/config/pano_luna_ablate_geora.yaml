defaults:
  - pano_luna.yaml
  - _self_

# Disable both Patch GeoRA and Camera GeoRA while retaining the same split
# windows, sampler, heads, losses, and pano-global-token setting.
exp_name: pano_luna_ablate_geora

model:
  enable_luna: False
