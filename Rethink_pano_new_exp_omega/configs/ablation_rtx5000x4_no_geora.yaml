# Disable Patch GeoRA and Camera GeoRA while retaining the split windows,
# pano-global token, heads, losses, curriculum, and mixed4 data schedule.
base_config: multipano_rtx5000x4_mixed4_pano_low_to_high_luna_after_full_warmup_9h.yaml

data:
  dataset_root: /mnt/e/PanoVGGT_minimal_datasets/datasets
  randomize_pano_order: false

model:
  disable_geora: true

runtime:
  debug_dir: logs/ablation_rtx5000x4_no_geora/debug
  tensorboard_dir: logs/ablation_rtx5000x4_no_geora/tensorboard

output:
  output_dir: logs/ablation_rtx5000x4_no_geora
